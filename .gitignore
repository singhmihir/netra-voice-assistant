# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Local secrets — never commit
.env
.env.local
*.pem

# Build artefacts
*.tsbuildinfo
__pycache__/
*.pyc

# Update-set work-in-progress (only the canonical NetraDeploymentV1.xml is tracked)
netra-snow/update-set/*.work.xml
netra-snow/update-set/*.tmp.xml
